"""
Models for the dashboard app.
Dashboard doesn't require its own models - it aggregates data from other apps.
"""
from django.db import models

# No models needed for dashboard - it uses data from other apps
